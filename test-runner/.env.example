# Test runner secrets (gitignored via root .env)

# Cursor SDK — create at https://cursor.com/dashboard/integrations
CURSOR_API_KEY=

# Optional: GitHub repo URL for cloud Cursor agents (can also set in UI)
# CURSOR_REPO_URL=https://github.com/you/content-manager
