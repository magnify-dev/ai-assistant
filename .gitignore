# Python
**/.venv/
**/__pycache__/
**/*.py[cod]
**/.pytest_cache/
*.egg-info/

# Voice assistant runtime
voice/.assistant.lock
logs/
*.log

# Open WebUI (installed locally via pip, not vendored)
open-webui/

# Third-party clone (reinstalled by setup-phase6.ps1)
helix-pilot/

# TTS binaries downloaded by setup
tts/piper/
tts/voices/

# Dev loop generated runs (not project .agent/)
dev_loop/.runs/

# Legacy — remove if present
.agent/

# Node / test-runner UI
node_modules/
test-runner/dist/
test-runner/dist-server/

# Secrets (keep *.example templates committed)
.webui_secret_key
.env
.env.local
.env.*.local
provided-variables
!.env.example
!**/.env.example

# OS / editor
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/

# Whisper / model caches (large, re-downloaded automatically)
**/.cache/

# Local image generation (ComfyUI clone + venv + outputs)
image-gen/.venv/
image-gen/ComfyUI/
image-gen/output/
image-gen/.cache/
